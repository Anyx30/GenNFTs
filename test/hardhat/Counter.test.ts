import { expect } from "chai";
import { ethers } from "hardhat";

describe("Counter", function () {
  it("Should pass this placeholder test", async function () {
    expect(true).to.equal(true);
  });
}); 